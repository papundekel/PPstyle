name: Release

on: 
  push:
    tags:
      - '*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Create package
        uses: Petkr/PPstyle/.github/workflows/package.yml@main
      - uses: actions/download-artifact@v2
        with:
          name: package
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: out-cpack/PPstyle-*.*.*-*.tar.gz
